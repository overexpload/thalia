/**
 * @desc request for sign in 
 * @route POST /api/signin
 * @access public
 */
module.exports = signin = async (req, res, next) => {
    try {
        
    } catch (error) {
        next(error.message);
    }
}

